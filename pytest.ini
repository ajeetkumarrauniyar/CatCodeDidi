[pytest]
testpaths = tests
addopts = -q
markers =
    gui: needs a working Tk display (skipped automatically when unavailable)
    live: hits the real Gemini API (skipped unless GEMINI_API_KEY is set)
